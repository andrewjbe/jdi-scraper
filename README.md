# JDI scraper

* Make sure you create `.env` in the root directory with variables `JDI_EMAIL` and `JDI_PASSWORD` set to Ryan's login
* Currently the script just runs once at the bottom over December dates in Oklahoma and Tulsa counties
